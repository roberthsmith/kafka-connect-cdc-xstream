version: "2"
services:
  oracle:
    image: docker.confluent.io:5000/oracle12c
    environment:
      ORACLE_SID: ORCL
      ORACLE_PDB: ORCLPDB1
      ORACLE_PWD: oracle
    ports:
      - '1521'
      - '5500'
    volumes:
      - './db/init:/db/init'